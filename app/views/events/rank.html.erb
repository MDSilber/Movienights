<h1> Rank movies for this event </h1>
<p>
  <b>Title:</b>
  <%= @event.title %>
</p>

<p>
  <b>Date:</b>
  <%= @event.date %>
</p>

<p>
  <b>Location:</b>
  <%= @event.location %>
</p>

<%= form_for(@event, :url => send_ranks_path ) do |f| %>
<p>
  <b>Please rank your preferences for the following movie choices:</b>
  <ul>
  <% @suggestions.each do |suggestion| %>
    <% @count = @suggestions.count %>
    <li><%= suggestion.movie.title %></li>
    <select>
    <% (1...@suggestions.count+1).each do |i| %>
      <option><%= i %></option>
    <% end %>
    </select>
  <% end %>
  </ul>
</p>

  <div class="actions">
    <%= f.submit "Send Ranks" %>
  </div>
<% end %>

<%= link_to 'Back', events_path %>
<%#= link_to 'Back', show_event_path(@event) %>
